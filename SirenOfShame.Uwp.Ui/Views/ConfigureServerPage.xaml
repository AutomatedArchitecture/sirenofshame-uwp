<local:PageBase
    x:Class="SirenOfShame.Uwp.Ui.Views.ConfigureServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SirenOfShame.Uwp.Ui.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource DefaultPageStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock 
            Grid.Column="1" 
            Style="{StaticResource PageHeaderTextStyle}" 
            Text="Configure" />

        <StackPanel Grid.Column="1" Grid.Row="1">
        <TextBlock x:Name="Title" TextWrapping="WrapWholeWords" Padding="5" Text="Configuring servers is currently only available via the web admin portal.\r\nPlease open a url to: {myUrl}"></TextBlock>
        
        <Button x:Name="DoneButton" Margin="0,20,0,0">Done</Button>
        </StackPanel>
    </Grid>
</local:PageBase>



